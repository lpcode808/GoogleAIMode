# G.AI Quick Launch

A mobile-first one-page launcher that appends `q=` to a base G.AI URL and opens the AI mode fast.

## Use

1. Open `index.html`.
2. Enter a search query.
3. Tap **Launch G.AI**.

Use **Settings** to change the base URL if Google updates the endpoint.
